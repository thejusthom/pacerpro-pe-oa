# pacerpro-pe-oa

Included the sumologic query in a text file, tf files in terraform and the lambda function in the folder
Did not know if we had to use sumlogic dashboard but ended up using it anyway